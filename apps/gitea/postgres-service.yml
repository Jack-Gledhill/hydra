apiVersion: v1
kind: Service
metadata:
  name: gitea-postgres
  namespace: apps
spec:
  selector:
    app: gitea
    component: postgres
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432