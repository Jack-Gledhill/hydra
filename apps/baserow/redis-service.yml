apiVersion: v1
kind: Service
metadata:
  name: baserow-redis
  namespace: apps
spec:
  selector:
    app: baserow
    component: redis
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: 6379