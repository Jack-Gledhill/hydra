apiVersion: v1
kind: Service
metadata:
  name: baserow-asgi
  namespace: apps
spec:
  selector:
    app: baserow
    component: asgi
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8000