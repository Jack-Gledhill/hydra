apiVersion: v1
kind: Service
metadata:
  name: baserow-wsgi
  namespace: apps
spec:
  selector:
    app: baserow
    component: wsgi
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8000