apiVersion: v1
kind: Service
metadata:
  name: baserow-postgres
  namespace: apps
spec:
  selector:
    app: baserow
    component: postgres
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432