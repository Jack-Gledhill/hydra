apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sealed-secrets
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.tls: "false"
spec:
  rules:
    - host: cert.hydra.starsystem.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sealed-secrets
                port:
                  number: 8080