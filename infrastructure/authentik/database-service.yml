apiVersion: v1
kind: Service
metadata:
  name: authentik-postgres
spec:
  selector:
    app: authentik
    component: postgres
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432