apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wopi
  namespace: traefik
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  tls:
    - hosts:
        - wopi.starsystem.dev
      secretName: wopi-tls
  rules:
    - host: wopi.starsystem.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sagittarius
                port:
                  number: 9300