apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: truenas
  namespace: traefik
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  tls:
    - hosts:
        - truenas.starsystem.dev
      secretName: truenas-tls
  rules:
    - host: truenas.starsystem.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sagittarius
                port:
                  number: 80