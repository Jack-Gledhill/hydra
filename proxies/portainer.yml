apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: portainer
  namespace: traefik
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  tls:
    - hosts:
        - portainer.starsystem.dev
      secretName: portainer-tls
  rules:
    - host: portainer.starsystem.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sagittarius
                port:
                  number: 9000