apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: autobrr
  namespace: traefik
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  tls:
    - hosts:
        - autobrr.starsystem.dev
      secretName: autobrr-tls
  rules:
    - host: autobrr.starsystem.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sagittarius
                port:
                  number: 7474