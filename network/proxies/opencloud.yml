apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: opencloud
  namespace: traefik
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  tls:
    - hosts:
        - cloud.starsystem.dev
      secretName: opencloud-tls
  rules:
    - host: cloud.starsystem.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sagittarius
                port:
                  number: 9200